# cvfored

# install:
```

pip install numpy
pip install dlib
pip install matplotlib
pip install --upgrade pip
pip install tensorflow-macos==2.12.0 keras==2.12.0 tensorboard==2.12.0 tensorflow-estimator==2.12.0

```